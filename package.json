{
  "name": "chess-analyzer",
  "private": "true",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "packageManager": "pnpm@10.4.1",
  "devDependencies": {
    "turbo": "^2.4.2"
  },
  "engines": {
    "node": ">=18"
  }
}
